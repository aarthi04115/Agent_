{
  "buildCommand": "cd mobile && npm install --legacy-peer-deps && npx expo export --platform web",
  "outputDirectory": "mobile/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
